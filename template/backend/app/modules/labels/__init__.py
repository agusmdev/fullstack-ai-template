"""Labels module — Label entity + IssueLabel association.

Internal namespace package. Import symbols from their submodules directly, e.g.
``from app.modules.labels.models import Label``. The ``__init__`` is intentionally
lightweight to avoid module-load circular imports with ``teams``.
"""
