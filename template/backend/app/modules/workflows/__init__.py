"""Workflows module — WorkflowState entity (issue status lifecycle).

Internal namespace package. Import symbols from their submodules directly, e.g.
``from app.modules.workflows.models import WorkflowState``. The ``__init__`` is
intentionally lightweight to avoid module-load circular imports with ``teams``.
"""
